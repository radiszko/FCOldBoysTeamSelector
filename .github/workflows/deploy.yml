name: 🚀 Deploy PROD

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Publish
        run: dotnet publish FCOldBoysTeamSelector/FCOldBoysTeamSelector.csproj -c Release --output publish

      - name: Set base href for PROD environment
        run: |
          sed -i 's|<base href="/" />|<base href="/FCOldBoysTeamSelector/" />|g' publish/wwwroot/index.html
          sed -i 's|var base = "/FCOldBoysTeamSelector";|var base = "/FCOldBoysTeamSelector/";|g' publish/wwwroot/404.html

      - name: Add .nojekyll
        run: touch publish/wwwroot/.nojekyll

      - name: Deploy PROD to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: publish/wwwroot
          keep_files: true
